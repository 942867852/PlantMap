# 2026-09-08

## 数据库 schema 升级：3 → 4

按 plantmap-db-change skill 流程，把数据库格式版本从 3 升到 4。

- **变更内容**：v4 相比 v3 新增 `SpeciesInfo` 的 `native_regions`（原生分布省区 adcode 列表）
  与 `habitat`（生境描述）两个字段——即 A1 地图功能的数据字段。数据模型/序列化/界面
  在之前的 A1 开发中已就位，本次只补「迁移」这一环。
- **迁移**：`dbversion.cpp` 新增 `migrateV3ToV4`（把 schema_version 置 4，并递归给每个节点
  的 info 补上空的 `native_regions=[]`、`habitat=""`，保证与“直接按 v4 保存”一致）；
  `kCurrentSchemaVersion` 3→4；`migrateToLatest()` 接入 version==3 分支。
- **自检**：selftest 新增 5 条（3→4 版本号推进、补 native_regions、补 habitat、迁移后的
  合法 v4 可加载、物种资料/拉丁名保留），并把链式迁移断言从 `==3` 改为 `==4`，
  层级校验用例的 schema_version 改用 `kCurrentSchemaVersion`。**69 项全过**。
- **兼容性结论**：旧库（v3 及更早）完全兼容，`loadFromFile` 先迁移再解析，磁盘文件
  不会被自动改写，用户保存时才落盘为 v4。已用新 core 层实测加载 bin/data/plantmap.json
  （265 节点 / 100 资料）成功。

改动文件：src/core/dbversion.h、src/core/dbversion.cpp、tests/selftest.cpp。

## 代码结构审查（重点：模块划分与可维护性）

按 plantmap-review skill 对 v0.3 当前实现做了结构审查，结论如下（均为可维护性建议，非功能性 bug）：

- **mainwindow.cpp 已达 1888 行、40+ 方法**，撤销/重做、收藏夹、复制节点、导出副本、
  导入、高级检索、编辑对话框全部堆在 MainWindow。建议拆分：
  - 撤销/重做（UndoCommand + 双栈 + applyUndoCommand）→ 独立 `UndoManager` 类；
  - 收藏夹（m_favorites + load/save/toggle）→ 独立 `FavoritesStore`；
  - SpeciesEditDialog（mainwindow.cpp:83 匿名命名空间的类）→ 独立文件。
- **撤销/重做与照片文件耦合在 UI 层**：applyUndoCommand 直接读写 photos/ 目录做照片
  恢复/清理，业务逻辑与文件 IO 交织，可考虑下沉到 core 或抽独立 helper。
- **docs/ARCHITECTURE.md 已过时**：仍写"最低一级是亚种""schema_version=3""变种/变型/品种
  在'未来扩展点'"，与当前实现（4 个并列末级、schema v4、新增 pinyin/map/lightbox 模块）
  严重脱节，需更新。
- 新增模块（photolightbox / province_map_widget / photoimageutils / pinyin）结构清晰、
  职责单一，划分良好，是本次扩展中的亮点。

### 本次同步更新 skill
- 更新 plantmap-review skill：补齐 v0.3 新增功能对应的审查点（地图可视化合规、
  拼音搜索、撤销重做、收藏夹、多图灯箱、拖拽/粘贴导入、复制节点、导出、草稿），
  并把"亚种是末级"等过时描述改为"亚种/变种/变型/品种四并列末级""schema v4"。

## 结构重构 + bug 修复（用户授权全做）

### 1. 更新 docs/ARCHITECTURE.md
对齐当前实现：等级链（种下四并列末级 亚种/变种/变型/品种）、schema v4、新增模块清单
（pinyin/province_map_widget/photolightbox/photoimageutils 等）、撤销重做章节、native_regions/habitat 字段。

### 2. 拆分 mainwindow.cpp（1888行 → 约 1500 行）
- `SpeciesEditDialog` → 独立 `specieseditdialog.h/.cpp`（原匿名命名空间类）
- `UndoManager` → 独立 `undomanager.h/.cpp`（UndoCommand 结构体 + 双栈 + apply + 照片恢复/清理）
- `FavoritesStore` → 独立 `favoritesstore.h/.cpp`（load/save/toggle/ids，持久化 favorites.json）
- MainWindow 改为组合这两个成员（m_favorites / m_undoManager），删除 pushUndo/applyUndoCommand/
  loadFavorites/saveFavorites 等旧方法。
- CMakeLists.txt 新增 3 组 .cpp/.h。编译通过，无 error/warning。

### 3. 修复的 bug
- **Bug：取消编辑后草稿残留**。SpeciesEditDialog::reject() 只 rollback + 删 session 照片，
  未清除草稿文件，导致下次打开同一植物再次弹"发现未保存草稿"。修复：SpeciesForm 新增
  public 方法 `discardDraft()`（复用 clearDraft），reject() 末尾调用它。
- **健壮性**：favoritesstore.h 补 `<QList>` include（之前靠间接包含）。

### 审查确认"非 bug"项
- cloneSubtree 副本共享原件照片文件名：删除原件时 collectAllReferencedPhotos 会保护副本
  仍引用的文件，不会误删，属合理设计（副本共享照片资源）。
- saveDraft 定时器 + showNode 切换：编辑窗口是模态的，编辑期间无法切换节点，无污染风险。

### 验证
- 编译通过（8 个 target），无 error/warning（仅 windeployqt 的 dxcompiler 环境警告）。
- plantmap_selftest.exe 退出码 0，无回归。

## 七大改进落地（功能 + 性能 + 便捷 + 健壮性）

用户要求一次性实现 7 项改进，全部完成并编译自检通过（EXIT=0）。

### 1. CSV 导出（csvexport.h/.cpp，core 层）
- 设计：固定列（等级/中文名/拉丁名/别名/光照/水分/温度/湿度/pH/土壤/耐寒区/习性/
  生命周期/叶型/生长速度/株高/冠幅/繁殖/用途/花期/果期/原生分布/生境/描述）
  + custom 扩展属性列自动推导（首次出现顺序收集），对属性变更免疫。
- UTF-8 带 BOM，CSV 字段逗号/引号转义。菜单"数据 → 导出 CSV…"。
- 决策：现在做（字段自动推导，属性后续变更导出自动跟随，无需重写）。

### 2. 批量操作（mainwindow）
- 树开启 ExtendedSelection 多选。
- 批量删除：去重父子节点（只删最上层），复用 removeSubtreeAndPhotos（每个节点独立 undo）。
- 批量设置属性：对话框选"属性+值"，支持习性/光照/水分/生命周期/叶型/生长速度/生境，
  每个植物独立 undo 命令。工具栏"批量设置属性… / 批量删除"。

### 3. 统计面板（statsdialog.h/.cpp）
- 四个 Tab：总览（节点数/植物数）、等级分布、习性分布、省区分布（Top20 条形图）。
- 工具栏"统计"按钮。

### 4. 物种对比（comparedialog.h/.cpp）
- 多选 2~5 个植物，属性行 × 植物列 表格对比（16 个关键属性）。
- 工具栏"对比"按钮。

### 5. 性能优化
- 照片缩略图缓存：photoimageutils 改 .h/.cpp，loadThumbnail/loadSearchIcon 加全局
  QHash 缓存（上限 512 张，超出清空），loadPreview 不缓存。导入后需 clearCache。
- 搜索防抖：onSearchTextChanged 拆出 runSearchDebounced，QTimer 200ms 防抖。
- 拖拽/粘贴导入异步化：addPhotosFromFiles 用 QtConcurrent::run 后台复制，
  完成后 QMetaObject::invokeMethod 回主线程更新列表。CMake 加 Qt6::Concurrent。
- 树增量重建：评估后跳过（风险大于收益，几百节点全量重建仅几毫秒）。

### 6. 操作便捷优化
- 记住窗口状态：QSettings 保存/恢复窗口 geometry + 分隔条状态（restoreUiState/saveUiState）。
- 快捷键：添加 Ctrl+N、编辑 Ctrl+E、重命名 F2、删除 Delete（撤销/重做原有 Ctrl+Z/Y）。
- 照片拖拽排序：编辑页照片列表改 InternalMove + Free 拖拽，顺序决定封面图。
- 最近打开：评估后跳过（收藏夹已覆盖需求，避免过度设计）。

### 7. 健壮性优化
- 数据库损坏自备份：loadFromFile 失败时立即把原文件备份为 plantmap.corrupt-时间戳.json。
- 图片解码兜底：已存在（loadScaled 返回空 QPixmap，调用方检查 isNull），无需额外处理。
- UI 层测试：selftest 新增 CSV 导出 3 条断言（固定列+自动列+逗号转义）。
- 日志系统：评估后跳过（个人桌面工具，qWarning 够用，完整日志框架收益低）。

### 改动文件
新增：src/core/csvexport.{h,cpp}、src/ui/statsdialog.{h,cpp}、src/ui/comparedialog.{h,cpp}、
photoimageutils 从 header-only 改 .h/.cpp。
修改：mainwindow.{h,cpp}（批量/统计/对比/CSV/防抖/窗口状态/快捷键）、speciesform.cpp（拖拽异步+排序）、
CMakeLists.txt（+Concurrent、新文件）、tests/selftest.cpp（CSV 测试）、docs/ARCHITECTURE.md。

## 提交推送 + 发布 v0.3.1

### git 提交（两次）
1. `8d4c9bf`：七大改进（CSV/批量/统计/对比 + 性能/便捷/健壮性优化），15 文件 +1120/-85。
2. `2bc941c`：版本号 0.3 → 0.3.1，README 更新，新增 RELEASE_NOTES_v0.3.1.md。

### 数据库变更检查结论：无变更
本次改动未触及 speciesinfo/taxondocument/dbversion 的数据模型或序列化，
schema_version 保持 4 不变，无需执行 plantmap-db-change 流程。

### 发布 v0.3.1
- CMakeLists VERSION 0.3.1；README 当前版本 + 更新记录；新建 RELEASE_NOTES_v0.3.1.md。
- 全库搜索版本号，历史 v0.3 引用（README 历史条目、RELEASE_NOTES_v0.3.md）不改；
  schema_version=4 未误改。
- 构建 + 自检通过（EXIT=0）。
- git：commit 2bc941c → push → tag v0.3.1（注解 tag，指向 2bc941c）→ push tag。
- 注意：注解 tag 的 `rev-parse v0.3.1` 返回的是 tag 对象 hash（a8f3fcf8），
  需 `v0.3.1^{}` 才解引用到 commit；创建 tag 报"already exists"是因为首次命令其实已创建成功。

### 关于 plantmap-db-change skill 的优化建议（本次未触发，静态评估）
- skill 的"自检要求"和"必须同步的 4 处"已较完整，但缺少一个**前置判断步骤**：
  "先确认本次改动是否真的改了存储格式（字段/结构/schema），纯读取类功能（如导出、
  统计、对比）无需走迁移流程"。可加一句"范围判断"开头，避免对只读功能误触发。
- 动态构建命令里 `--target PlantMap plantmap_selftest` 与 release skill 一致，无冲突。

## data 入库 + 新增 305 种北方常见植物（78b202f 已推送）

### 1. bin/data 纳入 git
- .gitignore 由 `bin/` 全忽略改为 `bin/*` + `!bin/data/**` 放行 + `bin/data/backups/` 仍排除。
- 提交内容：plantmap.json（405 种）+ photos/（99 张 20MB）。backups 临时备份不入库。

### 2. 数据扩充 100 → 405 种
- 新增 305 种北方常见植物，并入现有界门纲目科属种分类树（非独立分支）。
- 覆盖类群：北方针叶树/杨柳桦木壳斗榆桑、蔷薇科/豆科/木犀科/忍冬科花木果树、
  菊科/唇形科/毛茛科/十字花科/蓼科/伞形科/石竹科/百合科/禾本科等草本、
  北方蔬菜（茄/葫芦/十字花/伞形）、沙生旱生（沙棘/梭梭/柽柳）、山野菜药用植物。
- 真实学名+科属+习性+花期+描述；photos 留空（用户确认）；与原有 100 种去重
  （学名唯一+中文名唯一）。

### 3. tools/ 数据合并引擎（可复现可扩展）
- merge_plants.py：解析嵌套 JSON → 按路径查/建分类节点 → 学名唯一校验 → 写回。
- data_batch1~7.py：约 300 条数据的 7 个批次文件。
- 后续再加植物只需写新批次并跑 `python tools/merge_plants.py tools/data_batchX.py -o bin/data/plantmap.json`。

### 4. 关键经验
- **品种/变型(rank=cultivar/form/variety)不能直接挂 genus 下**：程序校验报
  "节点等级不合法"，品种必须挂种下或直接用 species。本次 12 个品种节点已全部转 species。
- 程序加载验证：临时 qt_add_executable + loadFromFile 确认 LOAD OK；
  selftest 无回归。临时验证 target 用完已移除。
- 学名冲突会被引擎跳过并报告（如已有的丁香/蜡梅/梧桐等不重复收录）。


## GitHub Release 上传 + 对比功能改造

### GitHub Release 上传 v0.3.1
- 本机无 gh CLI，从 Git Credential Manager 取到 GitHub OAuth token（gho_...）调 API。
- 打包 bin 为 PlantMap_v0.3.1_win64.zip（27 文件 29.6MB，排除 selftest.exe）。
- 创建 Release（id=384426025）并上传成功：https://github.com/942867852/PlantMap/releases/tag/v0.3.1
- 注意：当时 bin/data/plantmap.json 只有 3 种植物测试数据（用户后续手动补了 100 种）。

### 对比功能改造（"加入对比" + 对比页删除）
用户反馈原对比（多选树节点→对比）不好用，改为：
- 详情页新增"＋ 加入对比"按钮（m_compareButton，checkable，与收藏/编辑并列），
  发信号 compareToggled(nodeId)。
- MainWindow 维护会话内 `QList<int> m_compareList`（上限 5 个），toggleCompare 增删。
- CompareDialog 改收 `QList<int>*` 指针，顶部每列一个"植物名 ×"标签按钮，
  点击即从列表移除并 rebuildTable() 即时刷新；底部提示文案。
- 删除节点时（removeSubtreeAndPhotos）同步清理对比列表中的子树 id（删除前收集 doomedIds）。
- 搜索跳转/收藏夹跳转处补齐 setCompared 同步按钮状态。
- 上限 5 个：超出时提示并把按钮复位。

改动文件：speciesview.{h,cpp}（+对比按钮/信号/setCompared）、mainwindow.{h,cpp}
（+m_compareList/toggleCompare/清理逻辑）、comparedialog.{h,cpp}（改指针+删除交互）。
编译 + 自检通过（EXIT=0）。未提交。

## 对比页删除同步修复 + 整体代码审查（plantmap-review skill）

### 对比页删除修复
- 删除改按植物 id 定位（原按下标，列表含无效节点会错位删错）。
- 删空显示空状态提示 + 关闭按钮（不再建空表格）。
- rebuildTable 每次重建 chips + 表格，删除选项与列同步消失。

### 整体审查发现并修复的 bug（3 个）
1. **undo/redo 后 UI 状态不同步（中）**：undo/redo 只 rebuildTree()（无选中）
   + showNode，没显式 rebuildTree(newId)/setFavorite/setCompared，
   导致树无当前项、工具栏按钮禁用、收藏/对比按钮停留旧状态。已修复。
2. **批量设置属性文案误导（中）**：setInfo 强制学名非空，hasInfo=false 节点无法
   setInfo，原实现跳过但按钮写"应用到 N 个"实际少改。已修复：预过滤无资料节点、
   空时明确提示、状态栏说明跳过数量。
3. **异步照片复制悬垂/孤儿（高，低概率触发）**：QtConcurrent 后台复制捕获裸 this，
   invokeMethod(this,...) 的 context 保护使回调在对象销毁后被 Qt 丢弃（不崩），
   但已复制文件成孤儿。已修复：QPointer self 检测，销毁则删除孤儿文件。

### 审查确认为"非 bug"项
- cloneSubtree 副本共享原件照片：collectAllReferencedPhotos 跨节点扫描保护了共享，
  不会误删（设计意图）。
- 单边范围显示 view/form 一致（rangeText 哨兵 -100 对齐）。
- reject 删除 session 照片无守卫：文件名含毫秒时间戳+nodeId 必然唯一，无跨节点
  冲突可能（概率为零）。
- 草稿恢复不校验照片文件：极端边缘 + 恢复后显示"缺失"已提示，不修。
- StatsDialog 每 tab collect 一次（4 次遍历）：100 种规模 <1ms，不修。

### plantmap-review skill 优化（本次写入）
- 撤销重做章节补：undo/redo 后需显式 rebuildTree(newId) + setFavorite/setCompared +
  refreshActionState（QSignalBlocker 不触发选择信号）。
- 新增"批量操作与物种对比"章节：setInfo 学名必填→无资料节点须预过滤防误导；
  对比列表按 id 删除不能按下标；删除节点同步清理对比列表子树 id。
- 新增"异步任务生命周期"章节：QtConcurrent 捕获 this 的悬垂/孤儿处理（QPointer）。

## 修复全部 bug（第二轮专项，共 7 项）

第一轮已修（前几轮）：对比页按 id 删除+空状态、undo/redo 后 UI 状态同步、
QPointer 孤儿文件保护、batchSetAttribute 无资料节点文案。
本轮新修：
1. **搜索态批量操作误触（中）**：batchSet/batchDelete/clone 按钮搜索激活时未禁用，
   会操作隐藏树的旧选择。修复：提升为成员 action，refreshActionState + onSearchTextChanged
   统一禁用；树加 itemSelectionChanged → refreshActionState 联动多选。
2. **导入后收藏/对比 id 残留（中）**：导入成功/失败还原后 m_favorites/m_compareList
   未重置，旧 id 指向新库错误节点。修复：FavoritesStore 加 clearAll()/removeIds()，
   导入两路径都清空收藏与对比。
3. **删除节点收藏死 id（低）**：removeSubtreeAndPhotos 只清 compareList 没清 favorites。
   修复：m_favorites.removeIds(doomedIds) 同步清理并写盘。
4. **batchSet habitat 空值清空误操作（中）**：生境为自由文本，选 habitat 但留空确定
   会清空所有选中植物的生境。修复：空文本拒绝并提示。
5. **clone 搜索态可用（低）**：cloneAction 提升为成员，搜索态禁用。
6. （同 1 合并）批量按钮需多选≥2 才可用，单选禁用。
7. 树多选变化新增 itemSelectionChanged 信号刷新批量按钮状态。

改动：mainwindow.{h,cpp}、favoritesstore.{h,cpp}。编译无警告，selftest EXIT=0。
未提交。

## 提交推送 + 发布 v0.3.2 + skill 打包上传功能

### git 提交（4 个，均已推送）
1. `757b468` fix: 对比功能改造 + 修复多处 bug（9 文件 +299/-38）
2. `3578920` docs: review skill 增补 + 记录工作
3. `0f3d40e` release: 发布 v0.3.2（CMake 0.3.1→0.3.2、README、RELEASE_NOTES_v0.3.2.md）
4. `4a4eebf` docs: release skill 新增打包上传功能

tag: v0.3.2（注解，指向 0f3d40e）已推送。

### plantmap-release skill 新增"打包上传 Release"功能
- 原"打包与上传"章节（仅说明+引导手动）升级为可执行流程：
  1. Python 打包 bin → zip（排除 plantmap_selftest.exe）
  2. git credential fill 获取 gho_ token（无 gh CLI）
  3. GitHub API 创建 Release（POST /releases，tag 关联）+ 上传资产
     （POST /uploads/.../assets?name=...）
  4. 收尾：删本地 zip；旧 release 覆盖用 DELETE/PATCH
- 版本号支持三位 vX.Y.Z；打包文件名 PlantMap_vX.Y.Z_win64.zip
- 用户级 skill（C:\Users\qqzz\.workbuddy\skills\plantmap-release）与仓库副本
  （D:\QtProject\PlantMap\.workbuddy\skills\plantmap-release）已同步。

### GitHub Release v0.3.2 已创建并上传
- 打包：PlantMap_v0.3.2_win64.zip，48.59MB（bin 含用户补的 100 种植物数据）
- Release id=384450141，asset id=549870422（state=uploaded）
- https://github.com/942867852/PlantMap/releases/tag/v0.3.2
- 下载：https://github.com/942867852/PlantMap/releases/download/v0.3.2/PlantMap_v0.3.2_win64.zip








