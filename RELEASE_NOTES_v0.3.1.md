# PlantMap v0.3.1 更新说明

## 新增功能

- CSV 导出：菜单「数据 → 导出 CSV」，一键把全部植物资料导出为表格（UTF-8 带 BOM，
  Excel 可直接打开）。属性列自动推导，后续新增属性时导出会自动跟随。
- 批量删除：左侧树支持多选（Ctrl / Shift），一键删除多个节点，每个节点可独立撤销。
- 批量设置属性：对多选植物一次性设置光照、水分、生长习性、生命周期、叶型、生长速度、生境。
- 统计面板：工具栏「统计」查看总览（节点/植物数量）、等级分布、生长习性分布、原生省区分布 Top 榜。
- 物种对比：多选 2~5 个植物，并排对比 16 项关键属性（光照、水分、pH、株高、花期等）。

## 体验改进

- 记住窗口大小与左右分隔条位置，下次打开自动恢复。
- 新增快捷键：添加下级分类 Ctrl+N、编辑资料 Ctrl+E、重命名 F2、删除 Delete。
- 编辑页照片支持拖拽排序，第一张即为封面图。
- 搜索输入防抖，连续输入不再逐字全量检索。

## 性能优化

- 照片缩略图全局缓存，切换植物时重复照片不再反复解码。
- 拖拽 / 粘贴导入图片改为后台复制，大量图片导入时界面不再卡顿。

## 问题修复

- 修复取消编辑后草稿文件残留，下次打开不再反复提示「发现未保存草稿」。

## 健壮性

- 数据库文件损坏无法解析时，自动把原文件备份为 `plantmap.corrupt-时间戳.json`，
  避免被后续保存覆盖丢失线索。

## 数据说明

v0.3.1 未改动数据库格式（仍为 `schema_version = 4`），与 v0.3 数据库完全兼容，
可直接在原数据上运行，无需迁移。
