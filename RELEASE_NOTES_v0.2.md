# PlantMap v0.2 更新说明

## 新增功能

- 高级检索：可按生长习性、生命周期、光照、水分、叶型、生长速度、
  花期月份等属性进行组合精确筛选
- 数据库导入：可选择外部 `plantmap.json` 导入数据库
  - 导入前自动备份当前数据库
  - 导入失败时自动恢复原有数据与照片
  - 旧版数据库自动迁移到当前格式
  - 高于当前软件版本的数据库会被拒绝
- 数据库格式版本迁移机制：支持 `1 → 2 → 3` 的链式升级
- 界面显示软件版本号 v0.2

## 体验改进

- 编辑窗口支持完整取消回滚：放弃修改时，新添加的照片也会被清理
- 添加照片失败时自动删除已复制的残留文件
- 花期/果期支持全年显示：12 个月全选时显示“全年”
- 数值范围支持单边显示：只填低值时显示“最低 xx”，只填高值时显示“最高 xx”
- 耐寒区同样支持单边显示
- pH 统一保留 1 位小数，避免出现长浮点数
- 编辑页与高级检索下拉框均屏蔽滚轮误操作（包括展开后的列表）
- 搜索激活时禁用树节点的增删改，避免误操作隐藏节点

## 问题修复

- 修复“没有修改却提示未保存”的 pH 浮点误差问题
- 修复花期月份全选时错误显示“未填写”的问题
- 修复搜索结果误操作树节点的问题
- 修复照片目录残留孤儿文件的问题

## 数据说明

v0.2 使用 `schema_version = 3`。打开旧版 v0.1 数据库时程序会自动迁移，
迁移完成后保存一次即升级为最新格式；建议升级前先自行备份 `data` 文件夹。
