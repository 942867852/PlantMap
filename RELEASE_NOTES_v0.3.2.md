# PlantMap v0.3.2 更新说明

## 新增功能

- 物种对比交互升级：详情页新增"＋ 加入对比"按钮，可把浏览中的植物逐个加入
  对比列表（上限 5 个）；对比页面顶部每个植物一个标签，点击 × 即可从对比中移除，
  表格与标签即时同步刷新，对比列表清空时显示空状态提示。
- 对比列表跨场景保持一致：从搜索、收藏夹跳转、撤销/重做、删除节点后，
  详情页的"加入对比"按钮状态都会自动同步。

## 体验改进

- 批量操作按钮在左侧树中多选 2 个以上植物时才可用，避免单选时误点无反馈。
- 批量设置属性时，未保存过资料（尚无拉丁学名）的植物会被明确提示跳过数量，
  不再出现"应用到 N 个"实际只改几个的误导。
- 搜索状态下，批量删除/批量设置/复制节点等针对分类树的操作统一禁用，
  避免误操作隐藏树中的旧选择。

## 问题修复

- 修复撤销/重做后树没有选中项、编辑/重命名/删除按钮被误禁用，
  以及收藏、对比按钮状态停留在旧节点的问题。
- 修复批量设置"生境"时留空会误清空所有选中植物生境的问题。
- 修复导入其他数据库后，收藏夹与对比列表残留旧编号、可能跳转到错误节点的问题；
  导入时自动清空收藏与对比，删除分类节点时同步清理对应收藏。
- 修复拖拽/粘贴导入大量照片时，窗口关闭后可能残留孤儿照片文件的问题
  （后台复制完成时若窗口已关闭会自动清理）。

## 数据说明

v0.3.2 未改动数据库格式（仍为 `schema_version = 4`），与 v0.3 / v0.3.1 完全兼容，
可直接在原数据上运行，无需迁移。
